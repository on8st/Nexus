[package]
publish = false
name = "ft2"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Safe Rust wrapper over libtempo's native FT2 codec (Decodium's fast slotted mode, decode + TX encode)"

[lints]
workspace = true

[dependencies]
tempo-fast-sys = { path = "../tempo-fast-sys" }
